(function(){
'use strict';
const $=id=>document.getElementById(id),fmt=n=>String(Number(n)).padStart(2,'0'),money=v=>Number(v||0).toLocaleString('ru-RU')+' ₽';
const URL='data/xray-runtime.json';let runtime=null,payouts=null,refreshing=false,lastGen='',panel={grid:true,combos:true,fact:true,archive:false,payouts:false};
async function fetchJSON(url){const r=await fetch(url+(url.includes('?')?'&':'?')+'ts='+Date.now(),{cache:'no-store',headers:{'Cache-Control':'no-cache'}});if(!r.ok)throw new Error(`${url}: HTTP ${r.status}`);return r.json()}
async function loadPayouts(){if(!payouts)payouts=await fetchJSON('keno-payouts-v1.json');return payouts}
function head(title,key,extra=''){return `<div class="xrayPanelHead"><div><b>${title}</b>${extra?`<small>${extra}</small>`:''}</div><button class="xrayFold ${panel[key]?'on':''}" data-xfold="${key}" type="button">▼</button></div>`}
function targetLabel(x){return `№${x?.targetDraw||'—'} · ${x?.targetDate||'—'} ${x?.targetTime||'—'}`}
function sourceLabel(x){return `№${x?.sourceDraw||'—'} · ${x?.sourceDate||'—'} ${x?.sourceTime||'—'}${x?.sourceColumn?` · столб ${x.sourceColumn}`:''}`}
function relationClass(s){return String(s||'').toLowerCase().replace('column','root')}
function gridHTML(f){
 const fact=new Set((f?.current20||runtime?.latestOfficial?.balls||[]).map(Number)),pred=new Set((f?.predicted20||[]).map(Number)),nei=new Set((f?.structure?.neighborFront||[]).map(Number));let out='';
 for(let level=0;level<9;level++)for(let c=1;c<=9;c++){const n=level*9+c;if(n>80){out+='<span class="xrayCell blank"></span>';continue}const cls=fact.has(n)&&pred.has(n)?'both':fact.has(n)?'current':pred.has(n)?'predicted':nei.has(n)?'neighbor':'';out+=`<button class="xrayCell ${cls}" type="button" data-xcell="${n}" title="Ст${c} · L${level}">${n}</button>`}
 return out;
}
function concentrationLabels(){return `<div class="xrayColHeads">${Array.from({length:9},(_,i)=>`<b>Ст${i+1}</b>`).join('')}</div>`}
function renderOverlay(f){const svg=$('xrayOverlay'),stage=$('xrayStage');if(!svg||!stage)return;svg.innerHTML='<defs><marker id="xrayArrow" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L7,3.5 L0,7 z"></path></marker></defs>';if(!f)return;const rect=stage.getBoundingClientRect();svg.setAttribute('viewBox',`0 0 ${rect.width} ${rect.height}`);for(const e of (f.movementEdges||[]).slice(0,10)){const a=stage.querySelector(`[data-xcell="${e.from}"]`),b=stage.querySelector(`[data-xcell="${e.to}"]`);if(!a||!b)continue;const ar=a.getBoundingClientRect(),br=b.getBoundingClientRect(),line=document.createElementNS('http://www.w3.org/2000/svg','line');line.setAttribute('x1',ar.left+ar.width/2-rect.left);line.setAttribute('y1',ar.top+ar.height/2-rect.top);line.setAttribute('x2',br.left+br.width/2-rect.left);line.setAttribute('y2',br.top+br.height/2-rect.top);line.setAttribute('class',`xrayArrowLine rel-${relationClass(e.type)}`);line.setAttribute('marker-end','url(#xrayArrow)');svg.appendChild(line)}}
function activityTable(rows,type){if(!rows?.length)return '';const label=type==='column'?'Ст':'L',key=type==='column'?'column':'level';return `<div class="xrayMoveTableWrap"><table class="xrayMoveTable"><thead><tr><th>${label}</th><th>T1</th><th>T2</th><th>T5</th><th>T10</th><th>T20</th><th>1→2</th><th>2→5</th></tr></thead><tbody>${rows.map(r=>`<tr><th>${label}${r[key]}</th><td>${r.t1}</td><td>${r.t2}</td><td>${r.t5}</td><td>${r.t10}</td><td>${r.t20}</td><td class="${r.d12>0?'up':r.d12<0?'down':''}">${r.d12>0?'+':''}${r.d12}</td><td class="${r.d25>0?'up':r.d25<0?'down':''}">${r.d25>0?'+':''}${r.d25}</td></tr>`).join('')}</tbody></table></div>`}
function structureHTML(f){const s=f?.structure;if(!s)return '';const asc=(s.adjacency?.ascTop||[]).slice(0,7),draw=(s.adjacency?.drawTop||[]).slice(0,7),edges=(f.movementEdges||[]).slice(0,10);return `<div class="xrayStructureBox"><div class="xrayStructureTitle"><b>Движение таблицы</b><span>без общего рейтинга · T1/T2/T5/T10/T20</span></div><div class="xrayRankLine"><b>Столбы концентрации:</b> ${(s.columnRank||[]).map(x=>`Ст${x}`).join(' → ')}</div><div class="xrayRankLine"><b>LEVEL:</b> ${(s.levelRank||[]).map(x=>`L${x}`).join(' → ')}</div><details><summary>Активность столбов концентрации</summary>${activityTable(s.concentrationColumns,'column')}</details><details><summary>Активность LEVEL</summary>${activityTable(s.levelRows,'level')}</details><div class="xrayNeighborGrid"><div><b>➡️ По возрастанию ASC</b>${asc.map(x=>`<span>${fmt(x.from)}–${fmt(x.to)} · ${x.strength.toFixed(2)}</span>`).join('')}</div><div><b>🎲 По выпадению DRAW</b>${draw.map(x=>`<span>${fmt(x.from)}→${fmt(x.to)} · ${x.strength.toFixed(2)}</span>`).join('')}</div></div><div class="xrayVortex"><b>Vortex / Tesla:</b> 1→2→4→8→7→5→1 &nbsp; · &nbsp; 3↔6 &nbsp; · &nbsp; 9</div><div class="xrayActualMoves"><b>Реальные структурные переходы к прогнозным 20</b>${edges.length?edges.map(e=>`<span class="rel-${relationClass(e.type)}">${fmt(e.from)}→${fmt(e.to)} · ${e.type}${e.strength?` ${e.strength.toFixed(2)}`:''}</span>`).join(''):'<small>Удержание структуры без новых переходов.</small>'}</div></div>`}
function nums20(nums,hits=null,mode='pending'){const H=hits?new Set(hits.map(Number)):null;return `<div class="xray20Nums">${(nums||[]).map(n=>`<span class="${H?(H.has(Number(n))?'hit':'miss'):mode}">${fmt(n)}</span>`).join('')}</div>`}
function comboRow(label,nums,hits=null,payout=0){const H=hits?new Set(hits.map(Number)):null;return `<div class="xrayComboRow"><b>${label}</b><div class="xrayComboRowNums">${(nums||[]).map(n=>`<span class="${H?(H.has(Number(n))?'hit':'miss'):'pending'}">${fmt(n)}${H?(H.has(Number(n))?'✓':'×'):''}</span>`).join('')}</div>${Number(payout)>0?`<strong class="xrayWin">🔥 ${money(payout)}</strong>`:''}</div>`}
function comboPanel(title,cls,nums,subtitle){return `<div class="xrayCombo ${cls}"><div class="xrayComboTop"><b>${title}</b><span>${subtitle}</span></div><div class="xrayComboNums">${(nums||[]).map(n=>`<span>${fmt(n)}</span>`).join('')}</div></div>`}
function forecastBlock(f){if(!f)return '<div class="msg">Жду следующий frozen-прогноз.</div>';return `<div class="xrayForecast20"><div class="xrayForecastTitle"><b>Прогнозные 20</b><span>на ${targetLabel(f)}</span></div>${nums20(f.predicted20,null,'pending')}</div><div class="xrayFourCombos">${comboPanel('COMBO-5A','c5',f.combo5A||f.combo5||[],'ASC-сборка')}${comboPanel('COMBO-5B','c5b',f.combo5B||[],'Vortex-альтернатива')}${comboPanel('COMBO-7A','c7',f.combo7A||f.combo7||[],'ROOT/LEVEL-сборка')}${comboPanel('COMBO-7B','c7b',f.combo7B||[],'DRAW/Vortex-ветка')}</div>`}
function factBlock(e){if(!e)return '';const h20=e.forecast20Hits||e.layerHits||[];return `<div class="card xrayFactBlock">${head(`Факт тиража №${e.factDraw}`,'fact',`${e.factDate||'—'} ${e.factTime||'—'} · столб ${e.factColumn||'—'}`)}<div class="xrayPanelBody ${panel.fact?'':'hiddenX'}"><div class="xraySubTitle">Все 20 фактических чисел</div>${nums20(e.factBalls, e.factBalls,'fact')}<div class="xraySubTitle">Прогнозные 20 · совпало ${h20.length}/20</div>${nums20(e.predicted20,h20)}<div class="xrayArchiveCombos">${comboRow('COMBO-5A',e.combo5A||e.combo5||[],e.combo5AHits||e.combo5Hits||[],e.combo5APayout||e.combo5Payout)}${comboRow('COMBO-5B',e.combo5B||[],e.combo5BHits||[],e.combo5BPayout)}${comboRow('COMBO-7A',e.combo7A||e.combo7||[],e.combo7AHits||e.combo7Hits||[],e.combo7APayout||e.combo7Payout)}${comboRow('COMBO-7B',e.combo7B||[],e.combo7BHits||[],e.combo7BPayout)}</div>${Number(e.totalPayout)>0?`<div class="xrayTotalWin">🔥 Итого выигрыш: ${money(e.totalPayout)}</div>`:''}</div></div>`}
function archiveItem(e){const h20=e.forecast20Hits||e.layerHits||[],total=Number(e.totalPayout||0);return `<div class="xrayArchiveItem"><div class="xrayArchiveTop"><div><b>На №${e.targetDraw} · ${e.targetDate||'—'} ${e.targetTime||'—'}</b><small>Источник: ${sourceLabel(e)}</small></div><span class="done">проверен</span></div><div class="xrayArchiveSection"><b>Факт · 20 чисел</b>${nums20(e.factBalls,e.factBalls,'fact')}</div><div class="xrayArchiveSection"><b>Прогноз 20 · ${h20.length}/20</b>${nums20(e.predicted20,h20)}</div><div class="xrayArchiveCombos">${comboRow('5A',e.combo5A||e.combo5||[],e.combo5AHits||e.combo5Hits||[],e.combo5APayout||e.combo5Payout)}${comboRow('5B',e.combo5B||[],e.combo5BHits||[],e.combo5BPayout)}${comboRow('7A',e.combo7A||e.combo7||[],e.combo7AHits||e.combo7Hits||[],e.combo7APayout||e.combo7Payout)}${comboRow('7B',e.combo7B||[],e.combo7BHits||[],e.combo7BPayout)}</div><div class="xrayArchiveFoot">Факт: №${e.factDraw} · столб ${e.factColumn||'—'}${total>0?` <strong>🔥 ${money(total)}</strong>`:''}</div></div>`}
function payoutRows(size){const m=payouts?.combination?.[String(size)]||{};return Object.keys(m).map(Number).sort((a,b)=>a-b).map(h=>`<span><b>${h}/${size}</b> — ${money(m[h])}</span>`).join('')}
function bind(){document.querySelectorAll('[data-xfold]').forEach(b=>b.onclick=()=>{panel[b.dataset.xfold]=!panel[b.dataset.xfold];render()})}
function render(){const root=$('xrayRoot');if(!root)return;if(!runtime){root.innerHTML='<div class="card"><h2>🔬 Рентген структуры</h2><div class="msg">Загружаю SERVER LIVE…</div></div>';return}const f=runtime.forecast,h=runtime.history||[],last=h[0]||null;root.innerHTML=`<div class="card xrayLiveCard"><h2>🔬 Рентген структуры</h2><div class="muted">SERVER LIVE · столбы концентрации · ASC/DRAW · Vortex/Tesla · прогноз ведётся автоматически.</div><div class="xrayLiveLine">LIVE ${runtime.latestOfficial?`№${runtime.latestOfficial.draw} · ${runtime.latestOfficial.date} ${runtime.latestOfficial.time}`:'—'} · история ${h.length}</div></div><div class="card">${head('Таблица 1–80 · столбы концентрации','grid',f?`Источник ${sourceLabel(f)} → прогноз на ${targetLabel(f)}`:'ожидаю прогноз')}<div class="xrayPanelBody ${panel.grid?'':'hiddenX'}"><div class="xrayLegend"><span class="current">Факт</span><span class="predicted">Прогноз</span><span class="both">Совпало</span><span class="neighbor">Соседняя зона</span></div>${concentrationLabels()}<div id="xrayStage" class="xrayStage"><div class="xrayGrid">${gridHTML(f)}</div><svg id="xrayOverlay" class="xrayOverlay"></svg></div>${structureHTML(f)}</div></div><div class="card">${head('COMBO','combos',f?targetLabel(f):'ожидаю SERVER LIVE')}<div class="xrayPanelBody ${panel.combos?'':'hiddenX'}">${forecastBlock(f)}</div></div>${factBlock(last)}<div class="card">${head('История Рентгена','archive',`${h.length} записей`)}<div class="xrayPanelBody ${panel.archive?'':'hiddenX'}">${h.length?h.map(archiveItem).join(''):'<div class="xrayEmpty">Пока нет завершённых прогнозов.</div>'}</div></div><div class="card">${head('Таблица выигрышей','payouts',`версия ${payouts?.version||'—'}`)}<div class="xrayPanelBody ${panel.payouts?'':'hiddenX'}"><div class="xrayPayoutCols"><div><b>COMBO-5</b>${payoutRows(5)}</div><div><b>COMBO-7</b>${payoutRows(7)}</div></div></div></div>`;bind();requestAnimationFrame(()=>renderOverlay(f))}
async function refresh(){if(refreshing)return;refreshing=true;try{runtime=await fetchJSON(URL);await loadPayouts();if(runtime?.generation!==lastGen){lastGen=runtime?.generation||'';render()}else if(!document.querySelector('#xrayRoot .xrayLiveCard'))render()}catch(e){console.error('XRAY',e);const root=$('xrayRoot');if(root&&!runtime)root.innerHTML=`<div class="card"><h2>🔬 Рентген структуры</h2><div class="msg">SERVER LIVE временно недоступен: ${String(e.message||e)}</div></div>`}finally{refreshing=false}}
window.notifyXrayDataUpdated=refresh;window.addEventListener('resize',()=>runtime&&requestAnimationFrame(()=>renderOverlay(runtime.forecast)));refresh();setInterval(refresh,30000);
})();
