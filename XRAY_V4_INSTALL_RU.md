# XRAY STRUCTURE V4 — готовый пакет

Пакет собран из предоставленного проекта и не отправлялся в GitHub.

## Что изменено

- Сохранён существующий раздел `Рентген`, без переделки всего приложения.
- Верхняя таблица теперь строго 9 столбов концентрации:
  - Ст1: 1,10,19,28,37,46,55,64,73
  - Ст2: 2,11,20,29,38,47,56,65,74
  - ...
  - Ст9: 9,18,27,36,45,54,63,72
- Факт = все 20 чисел.
- Frozen-прогноз на следующий тираж = ровно 20 уникальных чисел.
- Из этих 20 формируются четыре отдельные комбы: 5A, 5B, 7A, 7B.
- Все четыре комбы обязаны быть подмножествами прогнозных 20.
- История Рентгена хранит:
  - факт 20;
  - прогноз 20 с зелёными совпадениями;
  - 5A, 5B, 7A, 7B отдельными ровными строками;
  - попадания/промахи;
  - 🔥 и выигрыш каждой выигравшей комбы;
  - общий выигрыш по записи.
- Движение строится из ASC / DRAW / столба концентрации / Vortex / LEVEL, а не из `abs(from-to)`.
- T1/T2/T5/T10/T20 остаются отдельными каналами; общего итогового рейтинга нет.
- Vortex/Tesla: 1→2→4→8→7→5→1, 3↔6, узел 9.
- SERVER LIVE больше не требует TensorFlow для каждого нового тиража: runtime пересчитывается чистым структурным движком.

## Версия

`4.3.3`

`refresh-combo-build.mjs` уже выполнен: обновлены cache-busting, manifest start_url и service-worker cache key.

## Проверка перед загрузкой

```bash
node --check xray-structure-engine-v4.mjs
node --check build-xray-runtime.mjs
node --check xray-ui-v1.js
node build-xray-runtime.mjs
node self-test-xray-v4.mjs
```

## Важно

В предоставленном архиве последний факт — №327248. Поэтому включённый `data/xray-runtime.json` заморожен на следующий №327249. После загрузки в репозиторий штатный updater должен догнать актуальный официальный архив и пересобрать Рентген на следующий реальный тираж.
