{
  "name": "INSTALL · COMBO Source Highlight · RETIRED",
  "on": {
    "workflow_dispatch": {}
  },
  "permissions": {
    "contents": "read"
  },
  "jobs": {
    "retired": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Historical installer is intentionally disabled",
          "run": "echo 'This one-time installer predates the immutable XRAY repair. Its script and Git history are retained. Use the current COMBO publisher for app/data/runtime updates.'"
        }
      ]
    }
  }
}
