{
  "name": "Apply Intervals nav button · RETIRED",
  "on": {
    "workflow_dispatch": {}
  },
  "permissions": {
    "contents": "read"
  },
  "jobs": {
    "retired": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Completed installer",
          "run": "echo 'Intervals navigation is already installed. Use the authoritative COMBO publisher for future app changes.'"
        }
      ]
    }
  }
}
